%div.header The clouds covered the sun before it was cool
%div.users
  %ul
    - @users.each do |user|
      %li= user.nickname

%div.messages
  - @messages.each do |message|
    %div.message
      %div.nickname
        = "#{message.user.nickname}:"
      = message.text
